# 한국 로또 6/45 분석 프로그램 PRD

## 1. 문서 목적

이 문서는 한국 로또 6/45 데이터를 수집하고, 통계적으로 분석하며, 참고용 번호 조합을 생성하는 프로그램의 1차 개발 범위를 정의한다.

이 프로그램은 로또 번호를 맞히는 예측기가 아니다. 로또는 무작위 추첨이며, 본 프로그램은 과거 데이터를 정리하고 통계적으로 살펴보는 도구이다.

모든 추천 조합과 리포트에는 다음 문구를 표시한다.

```text
본 결과는 통계 분석 기반 참고자료이며
당첨을 보장하지 않습니다.
```

## 2. 개발 목표

1차 목표는 너무 큰 AI 플랫폼이 아니라, 실제로 완성 가능한 로또 데이터 분석 프로그램을 만드는 것이다.

핵심 목표:

- 동행복권 회차 데이터를 수집한다.
- 데이터를 SQLite, CSV, JSON으로 저장한다.
- 번호별 출현 통계를 계산한다.
- 홀짝, 고저, 합계, 연속번호 같은 패턴을 분석한다.
- 간단한 점수화 방식으로 Hot, Warm, Cold 번호를 분류한다.
- 사용자가 조건을 지정해 참고용 조합을 생성한다.
- 추천 조합을 회차별로 저장하고 다음 회차 발표 결과와 비교 평가한다.
- 과거 회차 기준으로 백테스트를 수행한다.
- 엑셀 리포트로 결과를 저장한다.

## 3. 1차 개발 범위

### 3.1 포함 기능

- 데이터 수집
- 데이터 저장
- 기본 통계 분석
- 패턴 분석
- 번호 점수화
- Hot/Warm/Cold 분류
- 조건 기반 조합 생성
- 추천 이력 저장
- 다음 회차 결과 비교 평가
- 백테스트
- 엑셀 출력
- 기본 PNG 차트 저장

### 3.2 1차에서 제외할 기능

아래 기능은 좋은 아이디어지만 1차 개발에서는 제외한다.

- 웹 대시보드
- FastAPI 서버
- Streamlit UI
- 머신러닝 모델
- XGBoost
- LightGBM
- 자동 최적화 루프
- 번호쌍 네트워크 시각화
- 고급 Heatmap
- GUI 프로그램

이 기능들은 프로그램의 기본 구조가 안정된 뒤 로드맵 단계에서 추가한다.

## 4. 개발 원칙

- 모듈화된 구조로 개발한다.
- 수집, 저장, 분석, 조합 생성, 리포트 기능을 분리한다.
- 모든 함수에는 역할을 설명하는 docstring 또는 주석을 작성한다.
- 파일, DB, 네트워크 작업에는 예외 처리를 포함한다.
- 분석 결과는 화면 출력에 묶지 않고 재사용 가능한 데이터 구조로 반환한다.
- 추천 조합은 항상 참고용이라는 고지 문구와 함께 출력한다.
- 미래 당첨을 보장하는 표현은 사용하지 않는다.

## 5. 기술 스택

1차 개발에 사용하는 기술:

- Python
- SQLite
- Pandas
- NumPy
- Matplotlib
- OpenPyXL

각 기술의 역할:

- Python: 전체 프로그램을 작성하는 기본 언어
- SQLite: 로또 회차 데이터를 저장하는 작은 데이터베이스
- Pandas: 표 형태 데이터를 분석하기 위한 도구
- NumPy: 숫자 계산을 효율적으로 처리하는 도구
- Matplotlib: 기본 그래프를 PNG 이미지로 저장하는 도구
- OpenPyXL: 엑셀 파일을 만드는 도구

## 6. 프로젝트 구조

```text
lotto_analyzer/
├─ PRD.md
├─ main.py
├─ config.py
├─ requirements.txt
├─ README.md
├─ data/
│  ├─ lotto.db
│  ├─ lotto.csv
│  └─ lotto.json
├─ collector/
│  ├─ __init__.py
│  └─ crawler.py
├─ database/
│  ├─ __init__.py
│  └─ db_manager.py
├─ analysis/
│  ├─ __init__.py
│  ├─ frequency.py
│  ├─ pattern.py
│  ├─ scoring.py
│  └─ backtest.py
├─ generator/
│  ├─ __init__.py
│  └─ combination.py
├─ report/
│  ├─ __init__.py
│  ├─ excel_export.py
│  └─ chart.py
├─ tests/
│  ├─ test_collector.py
│  ├─ test_database.py
│  ├─ test_frequency.py
│  ├─ test_pattern.py
│  ├─ test_scoring.py
│  ├─ test_generator.py
│  └─ test_backtest.py
└─ logs/
```

## 7. 데이터 수집

### 7.1 수집 항목

- 회차
- 추첨일
- 당첨번호 6개
- 보너스 번호

### 7.2 수집 요구사항

- 단일 회차 수집을 지원한다.
- 여러 회차 범위 수집을 지원한다.
- 최신 회차까지 자동 수집할 수 있어야 한다.
- 이미 저장된 회차는 중복 저장하지 않는다.
- 수집 실패 시 프로그램이 종료되지 않고 오류 메시지를 남긴다.

### 7.3 데이터 검증

- 회차는 1 이상의 정수여야 한다.
- 당첨번호 6개는 1~45 사이의 서로 다른 정수여야 한다.
- 보너스 번호는 1~45 사이의 정수여야 한다.
- 보너스 번호는 당첨번호 6개와 중복되면 안 된다.

## 8. 데이터 저장

저장 방식:

- SQLite
- CSV
- JSON

SQLite 테이블명:

- `draws`

테이블 컬럼:

| 컬럼 | 설명 |
| --- | --- |
| draw_no | 회차 |
| draw_date | 추첨일 |
| num1 | 당첨번호 1 |
| num2 | 당첨번호 2 |
| num3 | 당첨번호 3 |
| num4 | 당첨번호 4 |
| num5 | 당첨번호 5 |
| num6 | 당첨번호 6 |
| bonus | 보너스 번호 |
| created_at | 저장 시각 |
| updated_at | 수정 시각 |

추가 저장 대상:

- 추천 이력
- 추천 대상 회차
- 추천 생성일
- 추천 전략
- 추천 조합 번호
- 조합 점수
- 발표 후 적중 개수
- 발표 후 평가 결과

출력 파일:

- `data/lotto.db`
- `data/lotto.csv`
- `data/lotto.json`

## 9. 기본 통계 분석

번호별로 다음 항목을 계산한다.

- 전체 출현 횟수
- 최근 10회 출현 횟수
- 최근 30회 출현 횟수
- 최근 100회 출현 횟수
- 최근 300회 출현 횟수
- 미출현 회차 수

출력 예시:

```text
번호 7

전체 : 183회
최근10회 : 4회
최근30회 : 8회
최근100회 : 19회
최근300회 : 54회
미출현 : 12회차
```

## 10. 패턴 분석

### 10.1 홀짝 분석

각 회차의 홀수와 짝수 개수를 계산한다.

예시:

- 6:0
- 5:1
- 4:2
- 3:3
- 2:4
- 1:5
- 0:6

### 10.2 고저 분석

기준:

- 저번호: 1~22
- 고번호: 23~45

예시:

- 3:3
- 2:4
- 4:2

### 10.3 연속번호 분석

연속된 번호 쌍을 찾는다.

예시:

- 7, 8
- 31, 32

### 10.4 끝수 분석

같은 끝수를 가진 번호가 얼마나 같이 나오는지 계산한다.

예시:

- 1
- 11
- 21
- 31
- 41

### 10.5 합계 분석

당첨번호 6개의 합계를 계산한다.

예시:

- 110
- 145
- 178

### 10.6 구간 분석

구간별 번호 개수를 계산한다.

구간:

- 1~10
- 11~20
- 21~30
- 31~40
- 41~45

## 11. 번호 점수화

번호별로 간단한 점수를 계산한다.

점수 항목:

- Frequency Score: 얼마나 자주 나왔는지
- Recency Score: 최근에 얼마나 나왔는지
- Gap Score: 얼마나 오래 나오지 않았는지
- Momentum Score: 최근 출현이 늘고 있는지

기본 공식:

```text
final_score =
frequency * 0.2 +
recency * 0.3 +
gap * 0.3 +
momentum * 0.2
```

주의:

- 이 점수는 당첨 확률이 아니다.
- 과거 데이터를 비교하기 위한 상대 점수이다.

## 12. 번호 분류

최종 점수를 기준으로 45개 번호를 분류한다.

- Hot: 상위 20%
- Warm: 중간 60%
- Cold: 하위 20%

분류 결과는 조합 생성과 리포트에 사용한다.

## 13. 조합 생성

사용자가 조건을 지정해 참고용 조합을 생성한다.

조건 예시:

```text
홀짝 = 3:3
고저 = 3:3
합계 = 100~180
연속번호 <= 1쌍
동일 끝수 <= 2개
```

지원 조건:

- 홀짝 비율
- 고저 비율
- 합계 범위
- 연속번호 최대 개수
- 동일 끝수 최대 개수
- 최근 당첨번호 제외 여부
- 생성 수량

지원 생성 수량:

- 1조합
- 5조합
- 10조합
- 50조합
- 100조합

생성 방식:

- Random: 순수 랜덤
- Balanced: 조건 균형 중심
- Hot Mix: Hot 번호 일부 포함
- Cold Mix: Cold 번호 일부 포함
- Hybrid: Hot, Warm, Cold 혼합

출력 예시:

```text
[3, 11, 17, 28, 34, 42]

점수 : 87
홀짝 : 3:3
고저 : 3:3
합계 : 135
Hot : 2
Warm : 3
Cold : 1

본 결과는 통계 분석 기반 참고자료이며
당첨을 보장하지 않습니다.
```

## 14. 추천 이력 및 다음 회차 평가

추천 조합은 생성 즉시 저장하고, 어떤 회차를 대상으로 만든 조합인지 기록한다.

예시:

```text
추천 대상 회차 : 1150
추천 생성일 : 2026-06-17
추천 전략 : Hybrid

추천 조합
[3, 11, 17, 28, 34, 42]
```

다음 회차 발표 후 실제 당첨번호와 비교해 평가한다.

예시:

```text
평가 대상 회차 : 1150
실제 당첨번호 : [3, 9, 17, 28, 35, 41]
보너스 번호 : 11

추천 조합 : [3, 11, 17, 28, 34, 42]
일반 번호 적중 : 3개
보너스 포함 적중 : 4개
결과 등급 : 5등 후보
```

저장 항목:

- 추천 ID
- 추천 대상 회차
- 추천 생성일
- 생성 전략
- 추천 번호 6개
- 조합 점수
- 홀짝 비율
- 고저 비율
- 합계
- Hot/Warm/Cold 구성
- 실제 당첨번호
- 보너스 번호
- 일반 번호 적중 개수
- 보너스 번호 적중 여부
- 평가 결과

평가 기준:

- 일반 번호만 기준으로 적중 개수를 계산한다.
- 보너스 번호는 별도 항목으로 표시한다.
- 로또 등수와 연결할 때는 실제 로또 6/45 등수 기준을 따른다.
- 평가 결과는 전략 개선 참고자료일 뿐 미래 당첨을 보장하지 않는다.

완료 기준:

- 다음 회차용 추천 조합을 저장할 수 있다.
- 새로운 당첨 결과가 수집되면 저장된 추천 조합과 비교할 수 있다.
- 회차별 추천 성과를 누적해서 확인할 수 있다.
- 엑셀 리포트에 추천 이력과 평가 결과를 포함할 수 있다.

## 15. 백테스트

과거 회차를 기준으로 조합 생성 전략을 검증한다.

예시:

```text
1000회까지의 데이터로 분석
1001회 참고 조합 생성
1001회 실제 결과와 비교
```

평가 항목:

- 3개 적중 횟수
- 4개 적중 횟수
- 5개 적중 횟수
- 6개 적중 횟수
- 평균 적중 개수
- 전략별 결과 비교

중요:

- 테스트 대상 회차 이후의 데이터는 사용하지 않는다.
- 백테스트 결과는 과거 기준 검증일 뿐 미래 결과를 보장하지 않는다.

## 16. 시각화

Matplotlib으로 기본 그래프를 PNG로 저장한다.

그래프:

- 번호별 출현 빈도
- 번호별 미출현 회차 수
- Hot 번호 순위
- 구간별 분포

출력 위치:

- `report/output/`

## 17. 엑셀 리포트

OpenPyXL로 엑셀 리포트를 생성한다.

출력 파일:

- `lotto_report.xlsx`

시트:

- 데이터요약
- 통계분석
- 패턴분석
- 번호점수
- 추천조합
- 추천평가
- 백테스트

추천조합 시트에는 반드시 고지 문구를 포함한다.

## 18. 개발 순서

### 0단계 PRD 작성

완료 기준:

- 요구사항 문서를 작성한다.
- 1차 개발 범위와 향후 로드맵을 분리한다.

### 1단계 데이터 수집

완료 기준:

- 동행복권 회차 데이터를 가져올 수 있다.
- 단일 회차와 범위 수집을 지원한다.
- 수집 데이터 검증이 가능하다.

### 2단계 DB 구축

완료 기준:

- SQLite 저장이 가능하다.
- CSV 저장이 가능하다.
- JSON 저장이 가능하다.
- 중복 회차 저장을 방지한다.

### 3단계 통계 분석

완료 기준:

- 번호별 기본 통계를 계산한다.
- 미출현 회차 수를 계산한다.

### 4단계 패턴 분석과 점수화

완료 기준:

- 홀짝, 고저, 연속번호, 끝수, 합계, 구간 분석을 계산한다.
- 번호별 점수를 계산한다.
- Hot, Warm, Cold 번호를 분류한다.

### 5단계 조합 생성

완료 기준:

- 조건 기반 조합을 생성한다.
- 생성 결과에 패턴 정보와 고지 문구를 포함한다.
- 다음 회차용 추천 조합을 저장한다.

### 6단계 다음 회차 평가와 백테스트

완료 기준:

- 발표된 다음 회차 결과와 저장된 추천 조합을 비교한다.
- 추천 조합별 적중 개수를 계산한다.
- 과거 회차 기준으로 조합 생성 전략을 검증한다.
- 미래 데이터 누수를 방지한다.

### 7단계 리포트 출력

완료 기준:

- 기본 차트를 PNG로 저장한다.
- 엑셀 리포트를 생성한다.

각 단계 완료 후 출력할 내용:

- 파일 구조
- 전체 코드
- 테스트 코드
- 실행 방법
- 다음 단계 진행 여부 확인

## 19. 향후 확장 로드맵

아래 기능들은 1차 개발 이후 추가한다. 지금 당장 만들 필요는 없지만, 나중에 프로그램이 커질 때 붙일 수 있도록 구조만 고려한다.

### 19.1 Streamlit UI

Streamlit은 Python 코드만으로 간단한 웹 화면을 만드는 도구이다.

쉽게 말하면, 터미널에서만 보던 분석 결과를 버튼, 표, 그래프가 있는 화면으로 보여주는 기능이다.

사용 예:

- 회차 수집 버튼
- 통계 분석 실행 버튼
- 추천 조합 생성 화면
- 엑셀 다운로드 버튼

1차 개발에서는 제외하고, 분석 로직이 안정된 뒤 붙인다.

### 19.2 FastAPI

FastAPI는 분석 기능을 외부에서 호출할 수 있게 해주는 API 서버 도구이다.

쉽게 말하면, 다른 프로그램이나 웹 화면이 `번호 통계 줘`, `추천 조합 만들어줘`라고 요청할 수 있는 통로를 만드는 것이다.

사용 예:

- 웹 대시보드에서 분석 결과 요청
- 모바일 앱에서 추천 조합 요청
- 다른 자동화 프로그램과 연동

혼자 쓰는 프로그램 단계에서는 꼭 필요하지 않다.

### 19.3 Plotly

Plotly는 마우스로 확대하거나 값을 확인할 수 있는 인터랙티브 그래프 도구이다.

쉽게 말하면, PNG처럼 고정된 그림이 아니라 사용자가 직접 움직여 볼 수 있는 그래프이다.

사용 예:

- 번호별 출현 빈도 그래프
- 회차별 합계 변화 그래프
- Hot 번호 순위 그래프
- 구간별 분포 그래프

1차에서는 Matplotlib으로 충분하고, 대시보드 단계에서 Plotly를 추가한다.

### 19.4 머신러닝 연구 모드

머신러닝 연구 모드는 당첨번호를 맞히기 위한 기능이 아니다.

과거 데이터에서 어떤 숫자나 패턴이 상대적으로 더 자주 등장했는지, 어떤 조건이 백테스트에서 더 안정적이었는지 살펴보는 실험 기능이다.

사용 예:

- 번호 중요도 확인
- 패턴 중요도 확인
- 조합 조건별 백테스트 비교
- 점수 가중치 후보 탐색

결과는 참고 지표일 뿐 미래 당첨 보장이 아니다.

### 19.5 Scikit-Learn

Scikit-Learn은 머신러닝 실험을 쉽게 할 수 있게 도와주는 기본 도구 모음이다.

쉽게 말하면, 데이터를 넣고 패턴을 비교하거나 중요도를 계산해보는 실험실 같은 도구이다.

사용 예:

- 번호별 중요도 실험
- 패턴별 중요도 실험
- 간단한 분류 모델 실험

1차 개발에서는 점수 공식과 백테스트만으로 충분하다.

### 19.6 XGBoost와 LightGBM

XGBoost와 LightGBM은 더 고급 머신러닝 모델이다.

쉽게 말하면, 여러 조건을 조합해서 어떤 요소가 결과와 더 관련 있어 보이는지 분석하는 강력한 실험 도구이다.

주의:

- 당첨번호 예측기로 사용하지 않는다.
- 번호 중요도와 패턴 탐색용으로만 사용한다.
- 기본 기능이 완성된 뒤 연구 모드에서만 검토한다.

### 19.7 Pair Network

Pair Network는 번호 두 개가 함께 나온 기록을 선으로 연결해 보는 시각화이다.

쉽게 말하면, `7번과 17번이 같이 나온 적이 많다` 같은 관계를 그림으로 보는 기능이다.

사용 예:

- 자주 같이 나온 번호쌍 확인
- 특정 번호와 자주 묶인 번호 확인
- 조합 생성 시 참고 지표로 사용

주의:

- 같이 자주 나왔다고 미래에도 같이 나온다는 뜻은 아니다.
- 통계적 참고자료로만 사용한다.

### 19.8 Heatmap

Heatmap은 숫자의 크기를 색으로 보여주는 표이다.

쉽게 말하면, 많이 나온 번호는 진한 색, 적게 나온 번호는 연한 색으로 표시하는 방식이다.

사용 예:

- 번호별 출현 빈도 Heatmap
- 회차별 번호 분포 Heatmap
- 번호쌍 동시 출현 Heatmap

데이터가 많아졌을 때 한눈에 흐름을 보기 좋다.

### 19.9 자동 최적화 루프

자동 최적화 루프는 여러 설정을 자동으로 바꿔가며 백테스트 결과를 비교하는 기능이다.

쉽게 말하면, 프로그램이 여러 조합 생성 조건을 시험해보고 과거 데이터에서 어떤 설정이 더 안정적이었는지 정리해주는 기능이다.

예:

```text
설정 A: Hot 2개, Warm 2개, Cold 2개
설정 B: Hot 3개, Warm 2개, Cold 1개
설정 C: Hot 1개, Warm 4개, Cold 1개

각 설정을 백테스트하고 결과 비교
```

주의:

- 스스로 미래를 맞히는 AI가 아니다.
- 과거 데이터에서 안정적이었던 설정을 찾는 실험 자동화이다.
- 과적합을 피하기 위해 여러 구간에서 반복 검증해야 한다.

### 19.10 GUI 프로그램

GUI는 사용자가 버튼과 입력창으로 프로그램을 사용할 수 있게 만드는 화면이다.

쉽게 말하면, 명령어를 입력하지 않고 일반 프로그램처럼 실행하는 방식이다.

가능한 형태:

- Streamlit 웹 UI
- 데스크톱 앱
- FastAPI와 프론트엔드를 분리한 웹 앱

1차에서는 CLI와 엑셀 출력으로 충분하고, 사용 흐름이 안정된 뒤 GUI를 붙인다.

## 20. 성공 기준

1차 개발 성공 기준:

- 회차 데이터를 수집할 수 있다.
- 데이터를 SQLite, CSV, JSON으로 저장할 수 있다.
- 번호별 통계를 계산할 수 있다.
- 주요 패턴을 분석할 수 있다.
- 참고용 조합을 생성할 수 있다.
- 추천 조합을 다음 회차 발표 결과와 비교 평가할 수 있다.
- 백테스트를 실행할 수 있다.
- 엑셀 리포트를 만들 수 있다.
- 추천 결과가 당첨 보장으로 오해되지 않도록 고지 문구를 포함한다.

장기 성공 기준:

- 사용자가 화면에서 쉽게 분석을 실행할 수 있다.
- 새 회차 데이터가 추가되어도 안정적으로 갱신된다.
- 전략별 백테스트 결과를 비교할 수 있다.
- 확장 기능을 추가해도 핵심 분석 로직이 흔들리지 않는다.
